kind = "web"
previewPath = "/"
title = "Island Tacos - Online Ordering"
version = "1.0.0"
id = "artifacts/island-tacos"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18184

[services.development]
run = "pnpm --filter @workspace/island-tacos run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/island-tacos", "run", "build" ]
publicDir = "artifacts/island-tacos/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18184"
BASE_PATH = "/"
